# 安全政策

目前项目处于初始开发阶段，安全修复面向默认分支和最新发布版本。

## 漏洞报告

请通过仓库的 **Security → Report a vulnerability** 私密报告可利用漏洞，包含受影响版本、最小复现和影响描述。若该入口未启用，请先开一个不包含漏洞细节的 Issue，请维护者提供私密沟通渠道。

不要在公开 Issue、PR 或日志中暴露可利用细节、数据库密码或令牌。

## 插件信任边界

只安装可信 Rust 源码。`dm install` 会运行 Cargo，包括插件及依赖的构建脚本、过程宏；插件执行拥有当前用户权限，并继承环境变量。独立进程不等于权限沙箱。

HTTPS 传输和 Cargo.lock 不证明插件可信。远程安装读取默认分支，尚未提供签名、固定 Git revision 或中央审核。需要审查具体版本时先克隆、检出和审查，再从本地路径安装。SDK 契约校验不构成来源认证。

插件存储目录应仅供当前用户写入。宿主不存储数据库凭证；具体插件负责自己的凭证处理策略。
